{
  "name": "inngest-server",
  "version": "1.0.0",
  "description": "Standalone Inngest server for Railway deployment",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "start": "node --import tsx/esm --tsconfig ../tsconfig.json server.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "inngest": "^3.44.2",
    "@prisma/client": "^6.11.1",
    "dotenv": "^16.5.0",
    "tsx": "^4.20.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
